version: '3'

services:
  mariadb1:
    image: mariadb:latest
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: password
#   volumes:
#     - mysql1:/var/lib/mysql

#volumes:
#  mysql1:
#    driver_opts:
#      type: tmpfs
#      device: tmpfs